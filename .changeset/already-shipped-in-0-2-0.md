---
---

Empty on purpose. The package changes on this branch are the ones already published
as 0.2.0 -- the version files say 0.2.0 and the registry serves it, so there is
nothing left to release. Changesets flags any package change without one, which is
the behaviour worth having; this records that the answer here is "already out".

Changesets starts keeping the ledger from 0.2.0 onward. The two releases before it
were hand-rolled version-bump commits, which is how 0.2.0 came to be on npm while
`main` still said 0.1.1.
